# Grid Affinity — Public assets

Public, non-secret assets used by Grid Affinity.

- `cdn/`: versioned level catalogs consumed through jsDelivr.
- `legal/`: legal documents intended for publication with GitHub Pages.

The application reads catalogs from:

`https://cdn.jsdelivr.net/gh/grid-affinity/public@main/cdn/`
